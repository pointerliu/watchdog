# Subscription Server

This crate provides a server implementation using the subscription framework. It exposes endpoints for managing subscriptions and handles the orchestration of fetching and notifying subscribers.

## Features

- REST API for subscription management
- Integration with the subscription framework
- Actix-based server implementation